<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.labmedixapplication">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" /> <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/splash"
        android:label="@string/app_name"
        android:roundIcon="@drawable/splash"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.ReminderListActivity"></activity>
        <activity android:name=".activities.UserVisitActivity" />
        <activity android:name=".activities.PolicyActivity" />
        <activity android:name=".activities.EditProfileActivity" />
        <activity android:name=".activities.AddReminderActivity" />
        <activity android:name=".activities.TermsAndConditionActivity" />
        <activity android:name=".activities.ReminderActivity" />
        <activity android:name=".activities.CheckUpPanelActivity" />
        <activity android:name=".activities.TestConditionActivity" />
        <activity android:name=".activities.UserInformationActivity" />
        <activity android:name=".activities.HomeLabrotoryActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".activities.MapsLabrotoryActivity"
            android:label="@string/title_activity_maps_labrotory" />
        <activity android:name=".activities.MapsActivity" />
        <activity android:name=".activities.AthunPhoneActivity" />
        <activity android:name=".activities.ListPanelActivity" />
        <activity android:name=".activities.LabInformationActivity" />
        <activity android:name=".activities.HomeActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.OverViewActivity" />
    </application>

</manifest>